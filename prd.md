# 句拼 · 产品设计（PRD）

> 技术选型与**为什么这么选**见 [spec.md](spec.md)。
> ⚠️ **数据模型与实现现状以代码为准**（表结构：[apps/server/src/db/schema.ts](apps/server/src/db/schema.ts)）。
> 任务与计划见 [plan.md](plan.md)，工程索引见 [AGENT.md](AGENT.md)。
>
> 规矩：**这个文件只回答「应该是什么」**（需求 / 名词 / 概念）。
> 能被代码回答的东西（字段、常量、阈值、表结构）不在这里复述 —— 抄一份就是第二份真相。

---

## 一、产品内核

**句拼 —— 每日英语朗读竞技场。**
英文朗读大比拼，AI 评分冲排名。

> ⚠️ 定位文案的唯一来源是 [packages/shared/src/brand.ts](packages/shared/src/brand.ts)。

### ⭐ 现状（做减法之后）

产品只有**三条核心概念**（外加一套由它们推导的成长体系），其余全部下线：

| 概念 | 说明 |
|---|---|
| **AI 反馈的得分** | 每天一句，云端评测给出总分 + 四维（准确 / 流利 / 标准 / 完整） |
| **排名** | 全站同题同尺：读同一句，比同一个分数 |
| **Streak** | 每天读一句 = 1 天连续；连续满 7 天得 1 张**解冻卡**，断档后由**用户主动**补签（不再自动消耗） |
| **成长值 ×3** | 自我超越 / 坚持不懈 / 人中翘楚 —— 以及 ⚡ 能量 与 ❄️ 解冻卡 |

> ⚠️ **等级徽章已整体废除**（上表里那一行删掉了）：它曾是「历史最长连续天数」的纯函数视图。
> 现在那一个数只剩展示用途；成长体系换成了三个成长值指标 + 能量 + 解冻卡。
> 规则与论证见 [docs/design/growth-and-energy.md](docs/design/growth-and-energy.md)，
> 实现与当前行为见代码。

**已下线**：v1 的难度分级（1–5 星 + 内部系数 D）、分类、积分、能力分曲线、实时逐词跟随、
端侧评分分析、自检页、**等级徽章**。

**已回归**：难度以**四档**（初级 / 中级 / 高级 / 专家）重新加入 —— 它是**正文的属性**
（与 `tags` 一起写在正文 JSON 里），不参与评分与任何判定，只用于展示与筛选。见下文「难度」。

---

## 二、两层结构

**内容单位 ≠ 竞技单位。这是整个设计的地基。**

```
┌─ 短文（100–300 词）──────────────────────────┐
│  · 全文 + 中文翻译                            │
│  · 整篇标准音（可听）                          │
│                                               │
│  其中切分出 3–5 个「竞技场」（自然句群）         │
│    ├─ 竞技场 1  ⭐⭐      1,284 人竞技          │
│    ├─ 竞技场 2  ⭐⭐⭐⭐    892 人竞技           │
│    └─ 竞技场 3  ⭐⭐⭐     1,057 人竞技          │
└───────────────────────────────────────────────┘
                ↓ 选一个竞技场
      录音（10–20 秒）→ 预检 → 提交检测 → 上榜
```

### 为什么必须分两层

| 若用整篇做竞技单位 | 后果 |
|---|---|
| 重录成本 | 读错一个词要重读 80 秒 → **核心循环崩溃** |
| 成本 | 引擎按文本长度计费，200 词 = 10 次调用 → 贵 10 倍 |
| 竞技场密度 | 门槛高、耗时长 → 榜单稀疏 |

**分层后的收益**：重录无痛 · 每次提交仅 1 次调用 · 一篇短文产出 3–5 个竞技场 · 同一篇内可有不同难度

---

## 三、用户只有两个动作

| 动作 | 成本 | 后果 | 限制 |
|---|---|---|---|
| **录音 / 重录** | 本地 | 无后果，不进榜，不消耗任何东西；录音本地缓存，提交不成也不丢 | **无限** |
| **提交检测** | 云端 | 打分 · **自动上榜** · 消耗 **2 点能量** | 能量不足则拒绝（每日惰性补足到 3 点） |

**没有"练习模式"，也没有"竞技模式"。**

### ⭐ 检测与上榜分离

**检测**（云端打分）与**上榜**（写进榜单）是两个动作——**先检测，后自动上榜，上榜不需要用户手工提交。**

- 检测返回「分数 + 主要问题」，用户据此决定要不要打磨重来；
- 上榜自动发生，用户无需纠结「要不要提交这份成绩」，榜单也因此更密；
- 结果页只有两个选择：**打磨重来**（重录，免费无限）或 **接受现状**（已自动上榜）。

---

## 四、核心循环

```
[ 进入短文 ]
   │ 看全文 / 听标准音 / 选竞技场
   ▼
[ 待录音 ]
   │ 录音（10–20 秒）
   ▼
[ 本地检测 ]
   ├─ 未通过（用户原因）→ 指出哪个词没听清 + 朗读提示 → [ 重录 ] ──┐
   ├─ 未通过（系统原因）→ [ 重新检测 ] ──────────────────────────┤
   └─ 通过 → [ 可提交 ]                                        │
                  │ 提交检测                                   │
                  ▼                                          │
            [ 返回结果 ] → 分数 · 主要问题 · 自动上榜                    │
                                                             └──► 回到 [ 待录音 ]
```

### ⭐ 关键前提：重录必须带反馈

否则就是**盲目重录**，用户在第二次就会放弃：

| | 体验 |
|---|---|
| ❌ "不合格，请重录" | 用户：哪里不合格？我该改什么？ |
| ✅ "girl 这个词没听清，再读一遍试试" | 用户知道往哪改 |

**只有带反馈的重录，才是练习。** 这就是为什么不需要"练习模式"——它已经在动作里了。

---

## 五、本地预检（放行标准）

**目的**：不达标的内容不得进入竞技场，也不消耗云端成本。

| 层 | 判据 | 动作 |
|---|---|---|
| **① 技术有效性** | 时长达标、检测到语音活动、能量正常 | 硬拦 |
| **② 长度合理性** | 有效语音时长 < 预期时长的 50% | 硬拦 |
| **③ 可识别性** | ASR 识别出的英文词 ≈ 0 | 硬拦 + 给提示 |

### 设计要点

- 判定标准是**「能不能被听懂」**，不是**「读得标不标准」**
  - 后者需要音素级模型，且误伤风险极高
  - 口音重、发音有瑕疵但能被听懂的学习者，**必须放行**——他们正是竞技场要服务的人
- 语速基准 **150 wpm**（每词约 0.4 秒）；**读得慢不会被拦**，只有明显没读完才拦
- 阈值一律**极度宽松**：放行一个垃圾的成本是几分钱，误伤一个用户的成本是流失
- **失败时保留"重新检测"出口**——应对网络/服务异常，避免死锁
- **只拦竞技场，不拦录音**——录音/重录永远无条件开放

---

## 六、指标体系

### 横向：竞技场

```
87 分
第 16 名 / 1,284 人
```

### 纵向：两层进步

| 层次 | 表达 | 本质 |
|---|---|---|
| **① 单场刷新** | `🎉 62 → 87` | **熟悉度**——我把这个竞技场练熟了 |
| **② 难度攻克** | 我攻克了「高级」的句子 | ⭐ **真实能力**——边界外扩了 |

> **第一层可能是幻觉**（多读几遍谁都能到 90），**第二层才是真的**——难句不会因为你熟悉就变简单。

### 攻克的定义

> **在这条句子上拿到分数 = 攻克。永久保留，只增不减。**

- ⚠️ **85 分线已废除**：攻克就是「出分了」，判据是提交状态 = `scored`
  （口径的唯一实现见 `apps/server/src/services/conquest.ts`）
- 按**去重的句子**计 —— 同一句读十遍只算一个攻克
- 用**绝对分**判定、**不用排名判定**
  - ❌ 排名判定会因"竞技场人数少"或"对手太强"而失真
  - ✅ 绝对分是同一把尺子，不受人数和人群水平影响
- **只有云端分可参与判定**

### 难度

**标在句子上**（句子 = 竞技场）。四档，不再有 1–5 星与内部系数 D。

| 档位 | 对外显示 |
|---|---|
| 0 | 初级 |
| 1 | 中级 |
| 2 | 高级 |
| 3 | 专家 |

- 与 `tags`（字符串数组）一起写在**正文静态 JSON** 里，**正文是唯一真相**
- ⚠️ 档位的数字取值与中文标签是**代码常量**（`packages/shared/src/difficulty.ts`），
  不要在这个文件里另立一份；库里的派生索引见 `apps/server/src/services/article-index.ts`
- 由内容流水线判定（步骤 ③）；**人没写就不写** —— 没有难度就是没有，
  绝不用默认档位顶上（编出来的难度比没有难度更糟）
- ⚠️ **目前不对外展示**（卡片上不放难度徽标）：字段先预留，服务端已经在卡片/详情接口里透出，
  端侧要显示时直接接，不用再动后端
- 不参与评分与任何判定：它只是描述，不是门槛

⚠️ **是「朗读难度」，不是「阅读难度」**——绕口令词汇简单但朗读极难，若按词汇定级会被严重低估。

⭐ **判级基准是「中国英语学习者」**（高中～大学，能看懂但发音受中文影响），**不是英语母语者**：
同一句对母语者轻而易举、对中国学习者却可能很难 —— /θ/ /ð/（think、this）、/v/、长短元音对立
（ship/sheep）、词尾辅音丛（-sks、-nths），以及**「必须连读才自然」**（而中国学习者习惯
一个词一个词字正腔圆地念，这恰恰是「读得不像」的主因）。
四档判的是：**这个人第一次看到、直接念出来，有多难念对**。

### 能力边界图（个人主页）

> ⚠️ **以下是 v1 设计，星级已废弃**（改为四档 初级/中级/高级/专家），这张图留作历史参考。

```
⭐⭐⭐⭐⭐  ░░░░░░░░░░   0
⭐⭐⭐⭐    ███░░░░░░░   3   ← 你在这里
⭐⭐⭐      ██████░░░░   6
⭐⭐        ████████░░   8
⭐          ████████████ 12
```

### 为什么不用能力分曲线

| | 能力分曲线（已废弃） | 能力边界图 |
|---|---|---|
| 表达 | "62 分"、"B1" | **"我能征服 ⭐⭐⭐⭐"** |
| 用户看懂了吗 | ❌ 需要理解加权公式 | ✅ **一秒就懂** |
| 目标明确吗 | ❌ "怎么从 62 到 70？" | ✅ **"去征服 5 个 ⭐⭐⭐⭐⭐"** |

---

## 七、能量与付费

### ⭐ 核心原则：封「检测」，不封「重录」

- **录音 / 重录**：本地、零成本、**永远无限免费**；录音本地缓存，提交不成也不丢。
- **检测**（云端打分）：唯一花钱的地方，**限量**；检测返回「分数 + 主要问题」，**上榜自动发生**（见第三节）。
- 一句话：**无限的是「练」，限量的是「评」。**

### 额度：能量（⚡）

> ⚠️ 这里以前写的是「每句 1 次免费额度 + 2 分钟挑战间隔」，那套**已整体换成能量**。
> 规则与论证见 [docs/design/growth-and-energy.md](docs/design/growth-and-energy.md)；
> 数值常量在 `packages/shared/src/constants`，结算法则在 `apps/server/src/services/energy.ts`。

- **每日惰性补足到 3 点**（没有定时任务）；**每次提交检测消耗 2 点**
- 余额可以**购买**（能量包），也可以由**奖励**得到（破全场纪录 +1 点）
- ⭐ **余额只有一份真相**：`users.energy` + `energy_ledger` 流水；
  扣减一律「受理时锁住、失败时释放」（净效果 = 失败不扣）
- 无效提交不消耗（见本节末「无效提交」）

### 无效提交

```
有效检测      → 消耗当天免费次数
判定无效      → 不消耗次数，计入"无效次数"
无效次数 ≥ 3  → 当天暂停提交
```

**理由**：用户不该因为一次技术故障丢掉当天机会；但要有护栏防刷。

---

## 八、已删除的概念（不要加回来）

| 概念 | 为什么删 |
|---|---|
| 能力分 P | 公式用户看不懂；"超过多少人"是更好的翻译 |
| 经验分 E | 白送的投入度，无成就感，诱导刷量 |
| 质量分 Q（对外） | 降级为竞技场原始分数，不再对用户解释 |
| 难度系数 D（对外） | 改为星级标签 |
| CEFR 等级 / 6 个称号 | 抽象，与竞技场语言不一致 |
| 三条曲线 | 由「能力边界图 + 单场打磨史」取代 |
| 总积分 | 累加值无意义；**绝不能做排行榜**（会退化成经验分） |
| 平均击败百分位 | 零和指标，分母会动，看不到真实进步 |
| 练习模式 / 竞技模式 | 用户只有两个动作 |

**用户需要理解的概念只有三个：第几名 · 刷新 · 征服。**

---

## 九、待验证清单

> ⚠️ **活的任务清单在 [plan.md](plan.md)**（见 C6「真机实验」）。
> 这一节原来是 5 条产品验证项，已收编过去 —— 本文件不再维护任务状态。
> 实验方案见 [docs/experiments/validation-experiment.md](docs/experiments/validation-experiment.md)。

唯一一条**方案级风险**仍记在这里：若「朗读难度不影响拿分难度」成立，
那么「按难度分层」在产品里就没有依据 —— 难度需要重新定位（这可能动摇它的存在本身）。
