/**
 * 核心常量。
 * ⚠️ 所有阈值只在这里定义一处，前后端共用，不允许多处硬编码。
 */

/** 征服阈值：竞技场得分 ≥ 此值即「征服」，永久保留 */
export const CONQUEST_THRESHOLD = 85

/**
 * ⭐ 挑战额度 —— **按句子算，不按时钟算**。
 *
 * ⚠️⚠️ 这里替换掉了原来的「每 24 小时滚动冷却」。为什么换：
 *    时钟冷却回答的是「你多久没来了」，而用户真正在意的是
 *    「**这句话**我还能再读几次」—— 同一句重读是练，不是刷。
 *    换成按句计数之后，「昨天读过、今天想再读一次」不再被拦，
 *    而「同一句反复刷到满意」有了明确上限。
 */
/** 免费用户**每句**可挑战的次数 */
export const FREE_ATTEMPTS_PER_SENTENCE = 1
/** 付费用户**每句**的硬上限（不是"无限"，避免脚本刷分） */
export const MEMBER_ATTEMPTS_PER_SENTENCE = 20

/**
 * ⭐ 两次挑战之间的**最小间隔**（毫秒）—— 防刷机，与额度无关，所有人一视同仁。
 * ⚠️ 它和「每句额度」是两件事：额度管"还能读几次"，它管"读得有多快"。
 *    只靠额度拦不住跨句连点（换一句就能绕过），所以两者都要有。
 */
export const CHALLENGE_INTERVAL_MS = 2 * 60 * 1000

/** 每日无效提交上限（超过则当天暂停提交） */
export const MAX_INVALID_PER_DAY = 3

/** 本地预检阈值 —— 一律极度宽松：放行垃圾的成本极低，误伤用户的成本是流失 */
export const PREFLIGHT = {
  /** 录音最短时长（毫秒）—— 低于此值判定为技术无效 */
  minDurationMs: 1500,
  /** 英语正常朗读语速（词/分钟），用于估算预期语音时长 */
  wpm: 150,
} as const

/** 单词发音时间估算（毫秒/词） */
export const MS_PER_WORD = 60_000 / PREFLIGHT.wpm

/** 单次评测的音频格式要求（讯飞 ISE 硬要求，不符会被判「乱读」） */
export const AUDIO_SPEC = {
  sampleRate: 16_000,
  bitDepth: 16,
  channels: 1,
  /**
   * 分析帧的采样数（1024 = 64ms）。
   * ⭐ 它与「录音 API 每次回调给多大一块」是**两个概念**，刻意解耦：
   *    回调帧多大由设备决定（实测 iPhone 请求 2KB 却给 4096 字节），
   *    需要固定长度分析时一律按这个值重新切（见 frameAudio）。
   */
  frameSamples: 1024,

  // ----------------------------------------------------------------
  // ⚠️ 传给 RecorderManager.start({ frameSize }) 的**请求值**
  //
  //    单位 KB，必须是整数；官方限制「暂仅支持 mp3、pcm」，我们用 PCM。
  //
  //    ⛔ 这**不是契约**：实测 iPhone 15 / iOS 26 请求 2KB 却拿到 4096 字节。
  //       推测是设备原生音频缓冲（iOS 常见 2048 采样 = 4096 字节）比请求值大，被向上取整。
  //       Android 尚未实测。
  //
  //    ⚠️ 所以：**不要用它推任何期望值**。曾经因为拿它当真，把正常的 4096 字节帧
  //       误判成 T2 失败。判断音频是否正确要看「每秒字节数」（见真机自检 T3+T4）。
  // ----------------------------------------------------------------
  frameSizeKb: 2,
} as const

/** 计费与定价 */
export const PRICING = {
  monthly: 19.9,
  yearly: 199.9,
} as const
