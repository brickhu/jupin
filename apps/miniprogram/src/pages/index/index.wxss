/**
 * 首页卡片的样式 —— 只有「圆形播放按钮」这一块是手写的。
 *
 * ⚠️ 为什么不像其余部分那样用 UnoCSS 工具类：
 *    圆 + 上下左右居中 + 描边 + 两种状态（待播 / 播放中）这套组合，
 *    用工具类要拼六七个类才能表达，而它在模板里出现两次（今日 + 往日）——
 *    手写一处、两处共用更不容易写歪。
 *
 * ⚠️ 播放中的变色用**继承**（.idx-play-on 改 color，图标 color: inherit），
 *    不用后代选择器：WXSS 的选择器支持面比 CSS 窄，少用一类少一个坑。
 */
.idx-play {
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  /* 待播：白底 + 品牌色描边与图标 */
  color: #4f46e5;
  background: #ffffff;
  border: 2rpx solid #4f46e5;
}

/* 播放中：实心品牌色 + 白色图标 */
.idx-play-on {
  color: #ffffff;
  background: #4f46e5;
}

/* ⚠️ color: inherit 是上面那套「变色只改父级」的关键，别改成固定色值 */
.idx-play-icon {
  color: inherit;
  font-size: 20rpx;
  line-height: 1;
}

.idx-play-hover {
  opacity: 0.7;
}
