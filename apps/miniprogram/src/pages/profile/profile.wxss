/**
 * 分享按钮 —— ⚠️ 必须是 <button open-type="share">：
 *    小程序里只有它能**主动拉起转发**，普通 view 点了不会弹分享面板。
 *
 * ⚠️ button 自带一整套默认样式（灰底、圆角、行高、::after 那圈边框），
 *    不重置的话它在卡片里会像一个坏掉的控件 ——
 *    与 pages/challenge 的 .cl-share 同一套处理，改一处记得看另一处。
 */
.pr-share {
  width: 72rpx;
  height: 72rpx;
  padding: 0;
  margin: 0;
  border-radius: 50%;
  background: #eeecfd; /* 与 uno.config.mjs 的 brand 同色系浅底 */
  display: flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}

/* ⚠️ ::after 是微信给 button 自带的边框，不干掉就会有一圈灰边 */
.pr-share::after {
  border: none;
}
/**
 * 还没加入时，头像那一格画的是「加入」按钮 —— 与导航栏那一格同一套判据
 *（服务端认不认识我，见 store 的 hasJoined）。
 *
 * ⚠️ 尺寸与头像**完全一致**（112rpx 圆）：两种形态共用一格位置，
 *    换形态时布局不跳，用户的视线也不用重新找。
 * ⚠️ 用品牌色实心圆而不是描边：这是这一页唯一「要你动手」的地方，
 *    描边那版看起来像一个未激活的控件。
 */
.pr-join {
  width: 112rpx;
  height: 112rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  background: #4f46e5;
  color: #ffffff;
  font-size: 28rpx;
  font-weight: 500;
  line-height: 1;
}
