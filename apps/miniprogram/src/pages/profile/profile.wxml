<!--
  ⭐ 「用户主页」—— 成绩墙（只读，对外展示）。

  ⚠️⚠️ 它在 pages 根目录、不在 pages/me/ 下：这一页是**给别人看**的
     （以后要能被分享、被陌生人打开），和 pages/challenge 同一个理由。
  ⚠️ 与 pages/profile-edit（**修改资料**）不是一回事：那一页是私有表单。
  ⚠️ 三个成长值**分开展示、不合成总分** —— 每个数各自回答一个问题，
     相加之后没人解释得清那个数是怎么来的。
-->
<view class="px-3 pb-[60rpx] bg-page min-h-[100vh] box-border" style="padding-top:{{navTop}}px">

  <nav-bar id="nav" title="我的主页" />

  <!-- ── 头部：头像 + 昵称 + 连战 ────────────────────────────── -->
  <view class="bg-white rounded-2.5 p-4 flex items-center">
    <image class="w-[112rpx] h-[112rpx] rounded-full bg-line" src="{{avatarSrc || avatarPlaceholder}}" mode="aspectFill" />
    <view class="ml-3 flex-1">
      <text class="text-34rpx font-semibold text-ink block">{{nickname}}</text>
      <text class="text-24rpx text-muted block mt-1">🔥 连续 {{streakDays}} 天 · 历史最长 {{streakBest}} 天</text>
    </view>
  </view>

  <!-- ── 成长值（三行，各自带一句解释）────────────────────────── -->
  <view class="bg-white rounded-2.5 p-4 mt-3">
    <text class="text-24rpx text-faint">成长值</text>
    <view class="mt-3 pt-3 border-t-[1rpx] border-t-solid border-t-line" wx:for="{{growthRows}}" wx:key="key">
      <view class="flex items-center">
        <text class="text-36rpx">{{item.icon}}</text>
        <text class="text-28rpx text-ink font-medium ml-2 flex-1">{{item.name}}</text>
        <text class="text-36rpx font-bold text-ink">{{item.value}}</text>
      </view>
      <text class="text-22rpx text-faint block mt-1 leading-[1.6]">{{item.blurb}}</text>
    </view>
  </view>

  <!-- ── 资产：能量 + 解冻卡 ─────────────────────────────────── -->
  <view class="bg-white rounded-2.5 p-4 mt-3 flex">
    <view class="flex-1">
      <text class="text-36rpx font-bold text-ink">{{energy}}</text>
      <text class="text-22rpx text-faint block mt-1">⚡ 能量点</text>
      <text class="text-20rpx text-faint block mt-1">每次挑战 2 点 · 每天补到 3 点</text>
    </view>
    <view class="flex-1">
      <text class="text-36rpx font-bold text-ink">{{unfreezeCards}}</text>
      <text class="text-22rpx text-faint block mt-1">❄️ 解冻卡</text>
      <text class="text-20rpx text-faint block mt-1" wx:if="{{unfreezeExpiresOn}}">最早一张 {{unfreezeExpiresOn}} 到期</text>
      <text class="text-20rpx text-faint block mt-1" wx:else>每连续 7 天得 1 张</text>
    </view>
  </view>

  <!-- ── 战绩入口 ────────────────────────────────────────────── -->
  <view class="bg-white rounded-2.5 mt-3 overflow-hidden">
    <view class="flex items-center px-4 py-[26rpx]" bindtap="onStreak" hover-class="active__opacity-60">
      <text class="text-28rpx text-ink flex-1">连战记录</text>
      <text class="text-24rpx text-faint">🔥 连续 {{streakDays}} 天</text>
      <text class="text-28rpx text-faint ml-2">›</text>
    </view>
    <view class="h-[1rpx] bg-line mx-4"></view>
    <view class="flex items-center px-4 py-[26rpx]" bindtap="onParticipations" hover-class="active__opacity-60">
      <text class="text-28rpx text-ink flex-1">参与场次</text>
      <text class="text-24rpx text-faint">{{participated}} 场 · {{conqueredCount}} 句拿到过分</text>
      <text class="text-28rpx text-faint ml-2">›</text>
    </view>
    <view class="h-[1rpx] bg-line mx-4"></view>
    <view class="flex items-center px-4 py-[26rpx]" bindtap="onChallenges" hover-class="active__opacity-60">
      <text class="text-28rpx text-ink flex-1">我的挑战</text>
      <text class="text-24rpx text-faint">{{rounds}} 次提交</text>
      <text class="text-28rpx text-faint ml-2">›</text>
    </view>
  </view>

</view>
