/**
 * 「我的挑战」页面自己的两小块样式。
 *
 * ⚠️ 其余一律用 UnoCSS 工具类写在 WXML 上（见 app.wxss 的说明）。
 *    这里留下的两块都是**工具类表达不了**的：
 *      · 圆形播放按钮（要圆形 + 居中，两三个类拼出来反而更难读）
 *      · 右侧渐隐遮罩 —— 它是一条 linear-gradient，
 *        写成 bg-[linear-gradient(...)] 既难看又依赖转义，不值得。
 */

/* ── 播放按钮 ─────────────────────────────────────────────── */
.cl-play {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: #eeecfd; /* 与 uno.config.mjs 的 brand 同色系的浅底 */
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  margin-right: 16rpx;
}

/* 正在播：实心底 —— 一眼能看出「响的是哪一条」 */
.cl-play-on {
  background: #4f46e5; /* theme.colors.brand */
}

.cl-play-glyph {
  font-size: 24rpx;
  line-height: 1;
  color: #4f46e5;
}

.cl-play-on .cl-play-glyph {
  color: #ffffff;
}

/* ── 句子：一行 + 右侧渐隐 ────────────────────────────────────
   ⚠️ 用「遮罩渐变」而不是 text-overflow: ellipsis：
      省略号会把最后一个词切成半个词（而那几个词是有颜色的，切一半很难看），
      渐变是「淡出」，读起来仍然完整。
   ⚠️ 遮罩是**一层绝对定位的渐变**，不是 mask-image：
      mask-image 要 -webkit- 前缀，各端 WebView 支持不一致且静默失效 ——
      渐变层在所有端上都成立，且这里底色固定是白的（卡片背景）。
   ⚠️ pointer-events: none —— 遮罩盖在最右边，不能把点击吃掉。 */
.cl-line {
  position: relative;
  flex: 1;
  min-width: 0;
  overflow: hidden;
}

.cl-fade {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  width: 80rpx;
  background: linear-gradient(to right, rgba(255, 255, 255, 0), #ffffff);
  pointer-events: none;
}