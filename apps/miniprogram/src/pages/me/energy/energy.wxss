/**
 * 「能量」页的样式。
 *
 * ⚠️ 这一页只有两处手写样式，其余一律用 UnoCSS 工具类（与全站一致，别在这里另起一套）：
 *    · 道具缩略图的方形圆角
 *    · 沙箱提示的浅黄底
 *    （余额那个大数字的行高用工具类 leading-[1] 表达，不在这里写）
 *
 * ⚠️⚠️ 注释里不要写反引号：小程序的 wxss 编译器会直接报 unexpected，
 *    整个样式文件不生效，而报错位置指向注释本身。
 *    要写代码标识符时用「」或直接写（见 build.mjs 与 wxss-lint.mjs）。
 */
.energy-thumb {
  width: 88rpx;
  height: 88rpx;
  border-radius: 20rpx;
  margin-right: 20rpx;
  flex-shrink: 0;
}

/**
 * 沙箱提示的底色。
 * ⚠️ UnoCSS 的 bg-warn/10 这种透明度写法全项目没用过，不确定 preset-weapp 支不支持 ——
 *    不值得为一行提示去赌一次构建，所以手写。颜色取 warn 的浅色版（amber-100），
 *    与连战日历那段手写颜色的理由相同。
 */
.energy-sandbox {
  background: #fef3c7; /* amber-100 */
}
