<!--
  ⭐ 「加入句拼」页 —— 用户从「一个打开小程序的人」变成「榜上有名字的人」的地方。

  ⚠️ 它是一整**页**，不是弹层。弹层那版出过两个具体问题：
     · 点头像授权时，微信自己的选头像浮层要盖在我们的 mask 上 —— 两层灰叠在一起
     · 键盘一弹，半屏面板被顶得七零八落
     页面没有这些问题：系统浮层盖的是页面，键盘把页面整体上推，各归各的。

  ⚠️ 表单本身在 components/profile-form 里，**和「修改资料」页共用同一个** ——
     真正要紧的不是那两行排版，是保存那一步（上传头像 → 落库 → 更新全局 state），
     抄第二份的那一刻两边就开始各自演化。这里只负责这一页自己的说法和去处。
-->
<!-- ⚠️ padding-top 由 navTop 给（= 导航栏总高 + 12），不要再写 pt-3 —— 见 lib/nav.ts -->
<view class="px-3 pb-[60rpx] bg-page min-h-[100vh] box-border" style="padding-top:{{navTop}}px">

  <!-- left 不填 = auto：栈深度 ≥ 1 时画「返回」 -->
  <nav-bar id="nav" title="加入句拼" />

  <!-- ⚠️ 这行说**它做什么**，不写抒情的话：
       "它会出现在榜单上"回答不了"我为什么要填"，
       "成绩记在这个名字下"才说明白了这个字段的作用 -->
  <text class="text-26rpx text-muted block px-1 mb-2.5 leading-[1.6]">头像和昵称会显示在榜单上，你的成绩都记在这个名字下。</text>

  <!-- autoFocus：新人进来就把微信昵称摆到键盘上方（见组件里的说明） -->
  <profile-form submit-label="确认加入" auto-focus="{{true}}" bind:saved="onSaved" />

  <text class="text-22rpx text-faint block mt-3 text-center">不会给你发任何消息，也不做别的用途。</text>
</view>
