/**
 * ⚠️ 和 user-sheet 一样：这里全部用普通类名，不用 UnoCSS 工具类 ——
 *    这一屏几乎全是「遮罩 + 从底部推上来」的定位与过渡，工具类在这里最容易静默失效。
 *    理由详见 nav-bar.wxss 顶部。
 */

.join-layer {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 300;
}

/* ⚠️ 不能用 inset: 0 简写 —— 老 WebView 不认，整条声明会被丢掉 */
.join-mask {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: rgba(0, 0, 0, 0.45);
  opacity: 0;
  transition: opacity 0.2s ease-out;
}

.join-mask-on {
  opacity: 1;
}

.join-panel {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
  background: #ffffff;
  border-radius: 32rpx 32rpx 0 0;
  padding: 0 48rpx 40rpx;
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
  transform: translateY(100%);
  transition: transform 0.22s ease-out;
}

.join-panel-on {
  transform: translateY(0);
}

.join-grab {
  width: 72rpx;
  height: 8rpx;
  border-radius: 8rpx;
  background: #e5e5e5;
  margin: 16rpx auto 0;
  padding: 12rpx 0;
  background-clip: content-box;
}

.join-title {
  margin-top: 20rpx;
  font-size: 36rpx;
  font-weight: 600;
  color: #111;
}

.join-sub {
  margin-top: 12rpx;
  font-size: 24rpx;
  line-height: 1.6;
  color: #999;
  text-align: center;
}

/* ⚠️ button 有自己的默认样式（边框、背景、行高），必须全部清掉。
   ⚠️ 高度**不能**写死：圆圈下面还有一行「用微信头像」，写死高度会把它裁掉。 */
.join-avatar-btn {
  margin: 32rpx 0 0;
  padding: 0;
  width: auto;
  height: auto;
  display: flex;
  flex-direction: column;
  align-items: center;
  line-height: 1;
  background: transparent;
  border: none;
  border-radius: 0;
}

.join-avatar-btn::after {
  border: none;
}

.join-avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  background: #eeecfd;
}

.join-avatar-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.join-avatar-plus {
  font-size: 48rpx;
  color: #4f46e5;
  line-height: 1;
}

.join-avatar-label {
  margin-top: 14rpx;
  font-size: 24rpx;
  color: #4f46e5;
}

.join-field {
  width: 100%;
  margin-top: 36rpx;
  background: #f7f7f8;
  border-radius: 16rpx;
  padding: 0 24rpx;
}

.join-input {
  height: 88rpx;
  font-size: 30rpx;
  color: #111;
}

.join-input-ph {
  color: #b9b9c0;
}

/* 昵称那一格下面的一行说明：告诉用户微信昵称就悬在键盘上，不用自己编 */
.join-hint {
  align-self: flex-start;
  margin-top: 12rpx;
  font-size: 22rpx;
  color: #b9b9c0;
}

.join-error {
  align-self: flex-start;
  margin-top: 16rpx;
  font-size: 24rpx;
  color: #dc2626;
}

.join-submit {
  width: 100%;
  margin-top: 40rpx;
  height: 92rpx;
  line-height: 92rpx;
  text-align: center;
  border-radius: 999rpx;
  background: #4f46e5;
  color: #ffffff;
  font-size: 32rpx;
  font-weight: 500;
}

.join-submit-off {
  opacity: 0.5;
}

.join-foot {
  margin-top: 24rpx;
  font-size: 22rpx;
  color: #bbb;
}
