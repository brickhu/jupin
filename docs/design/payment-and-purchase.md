# 购买与支付（设计稿）

> **状态：待确认 —— 确认后才开工**（要你拍板的项已收进 [plan.md](../../plan.md) A2–A5）。
> 与《成长体系与能量值》里的能量一节配套：那一份管「能量怎么来、怎么用」，这一份管「怎么买」。
> 相关：[growth-and-energy.md](growth-and-energy.md)、[reward-system.md](reward-system.md)
>
> ⚠️ **本文不是任务清单，也不是现状说明。**
> · **做没做、做到哪了 → [plan.md](../../plan.md)**（唯一任务来源）。
>   本文里的「待定 / 待你确认 / 执行清单 / 测试清单」都是**写作当时的快照**，与 plan.md 不一致时以 plan.md 为准。
> · **当前实际行为 → 代码及其注释**（唯一真相）。本文描述的是**设计意图**。
> · 下面 §11 的「开通执行清单」是**操作步骤**（怎么做），保留在这里；**做了没有**看 plan.md。

## 0. 一页看完

| | 内容 |
|---|---|
| **卖什么** | **能量包**：10 / 300 / 3000 点。解冻卡以后走**同一条路**（商品表用 kind 区分，接口不用再改） |
| **走哪条通道** | ⚠️ **微信「小程序虚拟支付」**（wx.requestVirtualPayment），**不是**云托管封装的普通微信支付 |
| **为什么** | 能量是**虚拟货币**。官方原文见 §1.1：小程序内的虚拟商品「购买和支付**均需**接入小程序虚拟支付」，适用**所有终端** |
| **商户号** | ⚠️ **不是现成的那个** —— 虚拟支付要**新开通一个二级商户号**，普通微信支付商户号不能用 |
| **商品形态** | **道具模式**（现金直购道具），**不用代币模式** —— 理由见 §1.3 |
| **发货** | 平台「发货推送」xpay_goods_deliver_notify → 幂等 → 加能量 + 写流水；推送丢了用 query_order 查单兜底 |
| **最大的三个坑** | ① iOS 最低支付 **1 元**、且微信道具**只能填整数元** ⇒ 三档一律按整元定（**¥1 / ¥20 / ¥180**）② iOS 退款**只能用户走 App Store**，我们不能主动退 ③ 所有请求要**签名**（appKey / sessionKey）|

---

## 1. 通道选型

### 1.1 ⭐⭐ 先看这条：虚拟商品必须走「虚拟支付」

微信官方《虚拟支付：企业、个体户》产品介绍原文：

> 为保障用户权益，提高交易安全，开发者在小程序内提供的**虚拟商品**（包括**虚拟货币**、解锁功能、
> 订阅内容、付费功能、打赏、购买虚拟礼物等），购买和支付**均需接入小程序虚拟支付**。
> 适用于 **Android、iOS、鸿蒙、Windows 等所有终端**，iOS 端需使用微信客户端 8.0.68 及以上版本。

来源：https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment.html

**我们的能量 = 虚拟货币**（花点数换挑战次数），所以：

- ❌ 不能用普通微信支付（wx.requestPayment）卖能量 —— 包括**安卓端**
- ❌ 不能引导用户跳外部支付
- ✅ 只能用**虚拟支付**（wx.requestVirtualPayment），它会在安卓走微信支付、在 iOS 走 Apple 支付

#### 1.1.1 ⭐⭐ 这不是「建议」，而是「平台会来关掉你的普通支付」

上面那段是**产品能力页**的说法（可以理解成「提供这个能力」）。真正把它变成硬约束的是
微信团队在微信开放社区发的《**虚拟支付业务运营指南**》（一手来源）：

> 1、什么是虚拟支付业务：开发者在小程序内提供的虚拟商品（包括虚拟代币、解锁功能、订阅内容、
> 付费功能、打赏、购买虚拟礼物等），购买和支付**均需接入小程序虚拟支付**；
> **不得引导至 app、公众号、h5、个人号、网站完成支付**。
>
> 3、典型虚拟服务场景，小程序**须全终端接入虚拟支付**……
>
> 4、**虚拟支付管控类目**：若小程序**仅包含以上虚拟类目**，平台将**关闭该小程序在安卓及其余
> 非 iOS 系统的普通微信支付能力**，需调用小程序虚拟支付能力进行交易。如小程序实际提供非虚拟
> 业务，如实物或线下交易等，可按实际经营业务申请其他类目，相关能力限制会自动解除。

来源：https://developers.weixin.qq.com/community/minihome/doc/00002cf077cd4810fee42f4b865c01

⭐ **关键在那句「平台将关闭安卓及其余非 iOS 系统的普通微信支付能力」**：
这条路不是「你想不想走」，而是平台会**主动关掉**普通支付 —— 所以「只做安卓、用普通支付省掉 iOS 的 12%」
这个算盘**打不响**。

**公告时间线**（二级来源，内容与上面的一手运营指南一致）：2026-02-27 微信团队发布规范更新公告，
要求涉及虚拟支付的小程序在 **2026-04-01 前全终端接入**，逾期「判定为违规」，
按程度采取**风险提醒 → 限制功能 → 暂停或终止提供服务**。
⇒ 这个截止日**已经过去**，我们现在不是「赶一个窗口」，而是**先把违规状态消掉**。
（来源：移动支付网对公告的转述 https://www.mpaypass.com.cn/news/202603/02164733.html ）

**我们落在哪个场景**：我们的服务类目是「**工具**」。

⚠️ 这一点要分清它**影响什么、不影响什么**：

| | 是否受影响 |
|---|---|
| 第 1 条「虚拟商品均需接入虚拟支付」 | ❌ **不影响**：判定看的是**卖的东西**（虚拟代币 / 付费功能），不是类目。我们卖能量 = 虚拟代币 |
| 第 4 条「关闭普通支付」的**触发条件** | ⚠️ **有影响**：触发条件是「小程序**仅包含以上虚拟类目**」。我们类目是「工具」，平台**未必**把我们判进管控类目 ⇒ 安卓的普通支付**可能暂时不会被关** |
| iOS 上卖虚拟商品 | ❌ **不影响**：这是一条**独立**的红线（Apple 的 App Store 规则要求数字内容/虚拟货币必须走 IAP），只能通过虚拟支付的 Apple 支付通道落地 |

⭐ 所以正确的读法是：

- **不能**把「我们是工具类目」当成「可以继续用普通支付」的依据 —— 第 1 条是行为规范，
  第 4 条只是**执行手段之一**（自动关能力）。执行没扫到 ≠ 不违规，而且平台的类目清单会变
- **能**确定的是：靠普通支付卖能量，最好的情况是「暂时没被关」，最坏是「限功能到终止服务」，
  而 **iOS 那条路从第一天起就走不通**
- 结论不变：**开虚拟支付**。而且工具类目 + 虚拟代币这套组合，正是官方虚拟支付要覆盖的目标形态

**有没有口子**（三条，我们都不适用）：

| 口子 | 适用条件 | 我们 |
|---|---|---|
| 豁免 | **线上问诊**（合法资质的医疗机构/医生实时远程诊疗）、**法律咨询**（持照律师/律所）| ❌ 不适用 |
| 混合型服务 | 同时含实物商品（如在线课程赠教材）—— 仍**被认定为虚拟支付**，建议拆两笔交易 | ❌ 我们没有实物业务 |
| 申请其他类目解除限制 | 小程序**实际提供非虚拟业务**（实物 / 线下交易）| ❌ 不适用 |

⭐ 结论：**开通虚拟支付是唯一正确的路，继续。**

### 1.2 那云托管封装的微信支付呢？

微信云托管确实封装了微信支付（POST http://api.weixin.qq.com/_/pay/unifiedOrder），免证书、免验签、
回调直接打到容器，看起来非常省事。但它**是普通微信支付**，只能卖给「实物 / 线下服务 / 物流类订单」。

| | 云托管封装的微信支付 | 小程序虚拟支付 ⭐ |
|---|---|---|
| 适用商品 | 实物、线下服务 | **虚拟货币 / 道具 / 会员 / 内容** |
| 商户号 | 我们已有的普通商户号 | **必须新开虚拟支付二级商户号** |
| 端侧接口 | wx.requestPayment | **wx.requestVirtualPayment** |
| iOS | ⛔ 不能用于虚拟商品 | ✅ 走 Apple 支付（Apple 佣金 12%） |
| 安卓费率 | 约 0.6% | 约 1%（腾讯技术服务费，2026 激励期） |
| 签名 | 云托管代签（免证书） | **自己签**：hmac_sha256(appKey, uri + '&' + signData) |
| 发货 | 自己处理回调 | 平台推送 + query_order 兜底 |
| 结算 | T+7 | **T+3**（iOS 由 Apple 结算，自然月后 45–60 天）|

⚠️ 云托管那条路的文档里也写明了它的定位：云托管在这里扮演的是「微信支付**服务商**」，
适合「支付收款、分账对账等基础能力」—— 它解决的是**怎么收钱**，不解决**能不能收这笔钱**。
合规边界是虚拟支付那条线划的，换通道不解决合规问题。

### 1.3 道具模式 vs 代币模式

虚拟支付给我们两种卖法，**选道具**：

| | 道具模式 ⭐ | 代币模式 |
|---|---|---|
| 商品在哪 | 微信侧「道具管理」里建道具（ID + 价格） | 微信侧「代币配置」里设代币名称 + 兑换比例 |
| 余额在哪 | **在我们自己库里**（users.energy） | **在微信侧**（query_user_balance / currency_pay） |
| 和现有能量模型 | 完全一致：买 → 加 energy + 写流水 | ⚠️ 会变成**第二份余额真相** |
| 改动 | 只加一个 xpay_product_id 映射 | 要重做「余额 / 扣减」两处，还要和 users.energy 对账 |
| 可改 | 道具可编辑后重新发布 | ⚠️ **代币兑换比例一旦发布不可修改** |

⭐ **决定性理由一：余额只能有一份真相。**代币模式会把余额搬到微信侧，
而我们的能量还有「每日补足 3 点」「奖励发点」「挑战扣 2 点」三种本地增减 ——
一旦有第二份余额，就必然出现「微信说 300、我们说 297」这种没法解释的差。

⭐⭐ **决定性理由二：代币只有一个兑换比例，表达不了我们的阶梯定价。**

代币配置要填的是「**1 元 = N 代币**」这么一个比例，而且**发布后不可修改**。
而我们的三档是**阶梯折扣**（买得多单价便宜）：

| 档位 | 售价 | 单价 |
|---|---|---|
| 10 点 | ¥1.00 | ¥0.100 / 点 |
| 300 点 | ¥20.00 | ¥0.0667 / 点（便宜 33%）|
| 3000 点 | ¥180.00 | ¥0.0600 / 点（便宜 40%）|

固定兑换比例**做不到**这件事：¥1.00→100 币、¥20.00→2000 币，单价一模一样。
想在代币模式里做阶梯，只能靠「充值赠送」（充 ¥20 之外再送一笔币，把单价压下来）——
那就变成了**每次充值都要额外调一次赠送接口**，还要自己处理赠送的幂等与失败，
而且「比例不可修改」意味着**以后想调价就锁死了**。
（道具模式天然支持：每个道具有自己的价格与数量，还能带 `origPrice`/`actualPrice` 做折扣。）

⭐⭐ **决定性理由三：能量有一大半是「发」出来的，而代币只能「买」。**

代币模式提供的动作只有三类：**查余额 / 扣减 / 赠送**，余额的语义是「用户自己充的钱」。
但我们的能量有一个代币根本没有的东西 —— **每日惰性补足**：

~~~
if (energyDate != today) { energy = max(energy, 3); energyDate = today }
~~~

⚠️ 注意是 **max 而不是「重置为 3」**（充值/奖励来的点数不能被每天抹掉），
而且它是**惰性**的（用户哪次来算哪次，不需要定时任务）。
这个语义在微信侧**压根不存在**：那边只有加减，没有 max，也没有「今天补过了没有」这种状态。
硬要用代币做，就得每天给所有活跃用户调一次赠送接口 —— 一个免费的日常动作
要跨网络、要幂等、要处理失败重试，而且**失败一次用户就少一次挑战机会**。

⇒ 结论：**能量住在我们自己库里**（users.energy + energy_ledger 流水），
微信侧只负责「收钱」这一件事；收完钱由 `deliverOrder` 把点数加进我们的账。

**那代币模式的好处呢？**它唯一的优势是「预充值更顺滑」（用户充一笔，站内反复消费，
不用每次拉起支付）。但那需要一个前提：**高频消费**。我们的节奏是**每天 1–2 次挑战**，
免费用户每天还自带 3 点 —— 顺滑度根本不是瓶颈，而上面三条代价都是永久的。
**道具模式下的体验是一样的**：买的是「能量包」，余额在我们这边，同样可以先攒后用。

### 1.4 ⭐ 那订阅（会员）算虚拟支付吗？

**算。**官方《虚拟支付：企业、个体户》把「**订阅内容**」和「虚拟货币」一起列进了虚拟商品清单，
而《会员订阅：安卓/鸿蒙》《会员订阅：iOS》则是虚拟支付**之上的一个子能力** ——
开通订阅的前置条件第一条就是「已接入小程序虚拟支付」。

⭐ 但这里有一个**决定要不要现在做包月**的区别，官方原话：

> 请注意此申请仅为开通**会员订阅自动续费**能力，**单次会员购买不需要开通会员订阅**！

⇒ 两条完全不同的路：

| | 单次买会员（不自动续费）| 会员订阅（自动续费）|
|---|---|---|
| 它是什么 | 就是一个**普通虚拟支付道具** | 虚拟支付的**订阅制子能力** |
| 要额外申请吗 | ❌ 不用，和能量包走同一条路 | ✅ 要发邮件申请开通「小程序订阅制」|
| 门槛 | 无额外门槛 | ⚠️ 见下表，很高 |
| 费率 | 与一次性购买相同（安卓约 1%）| 首笔 1%（工具/社交优惠），**第二笔起 10%**；iOS 12% |
| 结算 | T+3 | 周会员 T+7、月/季/年 T+31 |
| 端侧接口 | wx.requestVirtualPayment | 安卓 wx.requestSubscribeSign / iOS wx.requestAppleSubscribeSign |
| 沙箱 | 支持 | ⚠️ **不支持沙箱**（env 只能为 0）|

**自动续费的申请门槛（官方原文，必须全部满足）**：

| 条件 | 安卓/鸿蒙（vips.html）| iOS（vip.html）|
|---|---|---|
| 已接入小程序虚拟支付 | ✅ | ✅ |
| 小程序首次发布时间 | **≥ 90 天** | **≥ 90 天** |
| 认证备案 | ✅ | ✅ |
| 近 30 天日均 DAU | **≥ 1000** | ⚠️ **≥ 1 万** |
| 主体类型 | ⚠️ 仅企业 / 政府机构 / 事业单位 / 社会组织（**不支持个人和个体工商户**）| 同左 |
| 其他 | 商户号客服电话需完成认证、提交完整交互图审核 | 同左 + 必须先开通 iOS 支付 |

⚠️ 还有两个容易忽略的点：

1. **扣费不是想扣就扣**：非首次扣费要求商户在 **T-2 日**发预扣费通知，T 日只能在 **07:10–21:50** 之间扣；
   超过 T+2 没扣成就得**再等一个订阅周期**才能重发预通知。⇒ 用户因为余额不足掉会员会是常态。
2. **订阅道具不可编辑、不可删除**，且要单独审核 1–3 天；安卓/iOS 的 product_id 双端通用。

⭐ **对句拼的结论**：包月这件事**先别碰自动续费**。

- 门槛直接卡死：DAU ≥ 1000（iOS 要 1 万）、发布满 90 天 —— 我们**现在一条都不满足**
- 就算满足了，续费费率 10% 是能量包（1%）的十倍，而月费多半也就是二十元上下
- 真要做，第一步应该是「**单次买 31 天会员**」：不需要申请、费率和能量包一样、
  结算还快（T+3）。等 DAU 真的够大了，再决定要不要自动续费

⚠️ 而这里还有一个**产品问题**比通道更值得先想清楚：免费用户每天已经补到 3 点（够 1 次挑战），
会员要卖什么才让人愿意每月付一次钱？能量包（按次付费）和会员（包月）是**两种商业模式**，
同时存在会让「我该买哪个」变成用户的问题。⇒ 建议：**先把能量包跑起来，包月的价值主张另立一份文档**。

---

## 2. 商品与定价

### 2.1 三档（你给的售价）

| 商品 | 点数 | 售价 | 单价 | 每次挑战（2 点）| 相对首档 |
|---|---|---|---|---|---|
| energy_10 | 10 点 | **¥1.00** | ¥0.1000 / 点 | ¥0.200 | — |
| energy_300 | 300 点 | **¥20.00** | ¥0.0667 / 点 | ¥0.133 | 便宜 33% |
| energy_3000 | 3000 点 | **¥180.00** | ¥0.0600 / 点 | ¥0.120 | 便宜 40% |

梯度是合理的：三档之间是 10× / 10× 的量级跨度，单价单调下降，最大档比首档便宜 40%。

⚠️ 三档**必须都是整元**：微信侧「道具管理」的导入模板明写「道具价格：**需为整数**，不超过 10000 元」，
而道具价格是安卓 / iOS 双端通用的唯一价格 —— 我们这边写小数、微信侧只能填整数的话，
发货对账会直接报 -15013。这条有单元测试守着（`packages/shared/src/goods.test.ts`）。

### 2.2 为什么价格是 1 / 20 / 180（两条硬约束）

⭐ **已定：¥1.00 / ¥20.00 / ¥180.00**（10 / 300 / 3000 点）。两条约束一起决定了它：

**约束一：iOS 最低支付金额 1 元。**官方 iOS 下单条件里写死的。
所以首档不能是 ¥0.99（< 1 元会在拉起 Apple 支付那一刻直接失败，
用户看到「支付失败」，我们收工单，而原因在文档的一句话里）。
道具价格**跨端通用**（官方：「开发者配置的道具、代币均为 Android、iOS 双端互通」），
所以也不能「安卓 ¥0.99、iOS ¥1.00」两套价。

**约束二：微信侧道具价格只能填整数元。**道具批量导入模板原文：
「道具价格：必填，**需为整数**，不超过 10000 元」。
⇒ 我们这边的 `priceFen` 也一律取整元（`% 100 == 0`），两边永远不会因为取整而对不上。

⚠️ 两个被否掉的方案（记录备查）：

- **iOS 隐藏首档** —— 会让 iOS 用户买不到最便宜的档，是最差的选择
- **¥19.9 / ¥169.9 保留小数** —— 微信侧填不进去；硬填 20/170 的话发货对账报 -15013

### 2.3 费率：⭐ 道具和代币**没有区别**

费率**不是**按「道具 / 代币」划分的。官方《技术服务费》页给的维度只有三个：
**行业类目 × 设备类型 × 支付方式**（主动支付 / 会员订阅自动续费）。

来源：https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment/devplan.html

**我们落在「工具」类目**（一、工具、社交、深度合成、文娱（非微短剧/互动微短剧/网络小说）及其他类目）：

| 设备 | 支付方式 | 标准费率 | 当前生效费率 |
|---|---|---|---|
| Android / 鸿蒙 / Windows | **主动支付**（= 我们的道具直购）| **10%** | ⭐ **1%** |
| Android / 鸿蒙 / Windows | 会员订阅（自动续费）| 10% | 首笔 1%，**第二笔起 10%** |
| iOS | 主动支付 / 订阅 | 17%（含 12% Apple 佣金）| **12%**（全部为 Apple 佣金）|

对照：微短剧 / 网络小说类目在安卓的视频号挂载场景是 10%（无优惠），
非视频号挂载是 5% —— **类目差**才是费率差，而我们是工具类目。

⚠️⚠️ 两条必须记住的：

1. **1% 是「当前生效费率」，标准费率是 10%。**官方页只写「当前生效费率」，**没有写截止日期**。
   ⇒ 做长期测算时**不要按 1% 算**，要按 10% 还能活。
   （按 10% 重算：单次挑战收入 ¥0.120–0.200，渠道费 ¥0.012–0.020，仍远高于评测成本。）
2. **iOS 的 12% 是 Apple 收的，和我们无关也躲不掉**；标准是 17%（含 Apple 12% + 腾讯 5%），
   当前腾讯那 5% 暂免 ⇒ **这 5% 以后可能会回来**。

⭐ 对「选道具还是选代币」这件事：**费率这一项给不出任何倾向** —— 两者都是「主动支付」。
决定权在 §1.3 那三条（余额真相 / 阶梯定价 / 每日补足）。

**但代币模式有一个「钱」上的差别（不是手续费）**：代币余额是**预收款** ——
用户充了钱没花，钱在我们账上而货还没交付；退款时要从代币余额里扣回，
对账还要多一层「已售未消费」的口径。道具模式是**一手交钱一手交货**，不存在这笔挂账。

### 2.4 单位经济（成本对照）

| 项 | 单次挑战 |
|---|---|
| 收入（按首档 ¥0.200 / 次，最大档 ¥0.120 / 次）| ¥0.120 – 0.200 |
| 讯飞 ISE 评测成本（30 秒音频一次，项目内调研结论）| ≈ ¥0.0015 – 0.006 |
| 渠道费 · 安卓（当前 1%，标准 10%）| ¥0.001 – 0.020 |
| 渠道费 · iOS（当前 12%，标准 17%）| ¥0.014 – 0.034 |

⇒ 即使按「最贵的评测 + 标准费率」算，毛利仍在 80% 以上。**打分成本不构成定价约束**，
真正的约束是「免费用户每天补 3 点」这个获客成本（一个人每天送 1–2 次评测）。

（讯飞单价来自 docs/research/speech-eval-vendor-comparison.md；
渠道费率来自官方《技术服务费》页；提现手续费该页未提及，以商户平台/合同为准。）

⚠️ 退款与手续费：官方原文「支付时间 180 天以内的退款，平台会退还手续费；
超过 180 天的退款，平台不退还手续费」。 ⇒ 大额订单拖久了再退，手续费是我们自己承担。

### 2.4 商品目录放哪：落库，不是常量

⭐ 结论：**落库（goods 表）+ 种子**，与 reward_rules 同一套模式。理由：

1. **改价不该发版**。小程序审核 1–3 天，促销/调价绑在发版上就废了
2. **价格有两个源头**（我们的展示 + 微信侧道具），落库才能集中对账
3. 与本项目已有的「奖励规则落库」是同一个判断：**能被运营改的东西不要写死在代码里**

⚠️ 但**下单时必须快照**（payments 里存 goods_code / 点数 / 金额）：
商品表以后改了，历史订单必须还是当时那个商品 —— 这就是「改配置不追溯」。

#### ⭐ 一个与 reward_rules 故意不同的地方：商品配置**每次启动按代码校正**

`ensureDefaultGoods` 不只是「缺了才插」，它还会把**点数 / 价格 / 文案 / 角标 / 排序**
按 `ENERGY_PACKS` 覆盖回去。这与奖励规则「只在缺的时候插」是相反的，因为约束不同：

| | reward_rules | goods |
|---|---|---|
| 那个数是什么 | 我们自己的业务参数 | **与微信侧道具价格必须一致**的价格 |
| 谁能改 | 运营改库 | 改代码 + 重新导入道具 |
| 只改库的后果 | 按新阈值发奖（无害）| ⚠️ **付不了款**：微信拿 signData 里的 goodsPrice 比道具价，报 -15013 |

⇒ 只改库里的价是**结构性坏掉**的（改完反而更糟），所以它必须以代码为准。
两样东西**不校正**：`enabled`（运营可以临时下架一档）、`xpay_product_id`（来自环境变量）。

⭐ 配套的自查：`/health` 的 `pay` 块里有 **`prices`**（读的是**库里实际存的值**）——
「代码 / 库 / 微信侧」三方是不是同一个数，一眼就能核对。

---

## 3. 数据模型

### 3.1 goods（新表）

| 列 | 说明 |
|---|---|
| code | 商品码，主键式唯一：energy_10 / energy_300 / energy_3000 |
| kind | **energy / unfreeze** —— 解冻卡以后不改接口，只加一行商品 |
| amount | 发多少（点 / 张）|
| price_fen | 售价，**单位分**（2000 = ¥20）。⚠️ 必须是整元，且与微信侧道具价格一致 |
| xpay_product_id | 微信侧道具 ID（道具管理里创建时拿到的）|
| title / subtitle | 展示文案（端侧只渲染，不判断）|
| badge | 角标文案，可空（如「省 33%」）。⚠️ 折扣类角标有测试盯着，改价时会先红 |
| sort / enabled | 排序与上下架 |

⭐ 端侧价格也从这里来（GET /api/shop/goods），不在小程序里写死 —— 否则改价要发版。

### 3.2 payments（已有表，要改）

现有列大多能用（out_trade_no / amount / status / transaction_id / raw_notify / refund 三兄弟），要改的是：

| 改动 | 说明 |
|---|---|
| plan → **goods_code** | plan 的语义是 monthly/yearly（旧包月模型），现在卖的是商品码。⚠️ 改名 + 迁移要拆两次 generate（drizzle-kit 会弹交互式 rename 提问）|
| 新增 **goods_kind / goods_amount** | 点数快照。以后商品改了，订单还是当时那个 |
| 新增 **xpay_order_id** | 微信侧单号（虚拟支付的 wx_order_id），对账/查单用 |
| 新增 **env** | 0 = 现网 / 1 = 沙箱。⚠️ 同一个 out_trade_no 在沙箱和现网是两个世界 |
| 新增 **delivered_at** | 货什么时候发的。paid 是「钱到了」，delivered 是「货发了」，两件事 |
| status 取值 | pending / paid / refunded / failed / closed |

### 3.3 energy_ledger（不用改，直接用）

⭐ 买能量就写一行：reason='purchase'、ref_type='purchase'、**ref_id=out_trade_no**、delta=+N。

它的唯一键 (reason, ref_type, ref_id, user_id) **天然就是发货的幂等键** ——
发货推送重推 15 次、我们并发处理，都只会加一次能量。这一点不需要新机制。

### 3.4 users（加一列）

| 列 | 说明 |
|---|---|
| **session_key** | 虚拟支付的**服务器 API 要用户态签名**（hmac_sha256(session_key, signData)）。现在服务端**完全没有处理过 session_key**（只用了 openid）|

⚠️ session_key 是敏感凭证（等同于登录态），且会过期；它只能从 auth.code2Session 拿。
⚠️ 我们的线上主通道是 callContainer（openid 由网关注入），**这条路上拿不到 session_key**。
⇒ 方案见 §5.2：需要时由端侧补一次 wx.login。

---

## 4. 完整链路

### 4.1 买一次能量要经过什么

~~~
① 端侧：用户在 me/energy 点「¥20 买 300 点」
② 端侧 → 服务端：POST /api/shop/order { goodsCode: 'energy_300' }
     服务端：查商品（必须在售）→ 生成 outTradeNo（= 幂等键）
             → 落一行 payments(status=pending, 快照点数/金额)
             → 调虚拟支付服务器 API 下单 → 拿 wx_order_id
             → 算 paySig / signData → 返回 payData
③ 端侧：wx.requestVirtualPayment(payData)
     · 安卓/鸿蒙/Windows → 微信支付拉起
     · iOS → Apple 支付拉起（用户需 iOS 15+ / 微信 8.0.68+ / 金额 ≥ 1 元）
④ 支付成功 → 平台向我们推送 xpay_goods_deliver_notify
     服务端：校验 → 幂等 → 一个事务里：
              payments.status = paid + xpay_order_id + paid_at + delivered_at
              users.energy += N
              energy_ledger 写一行（幂等键 = out_trade_no）
     返回 {"ErrCode": 0}
⑤ 端侧：支付 success 回调 → 只做「提示 + 刷新余额」，**不作为发货依据**
~~~

⚠️⚠️ 端侧的 success 回调**可能丢失**（微信异常退出），官方原文要求
「发货以发货推送为主，推送丢失时用 query_order 查单兜底」，
并且明确「**前端支付回调不作为发货依据**」—— 绝不能靠它发货。

### 4.2 服务端接口（我们的）

| 接口 | 作用 |
|---|---|
| GET /api/shop/goods | 商品列表（含价格、点数、角标）。端侧渲染充值卡片 |
| POST /api/shop/order | 下单，返回 payData |
| POST /api/shop/order/:outTradeNo/check | 「我付了但没到账」→ 主动查单补发 |
| GET /api/user/energy | 余额 + 流水（分页）—— me/energy 页用 |
| POST **/api/pay/notify** | ⭐ 平台发货推送的落点。**必须在鉴权中间件之外**（平台不带我们的 token）|

⚠️ /api/pay/notify 是本项目**第一个不在 authMiddleware 后面的写接口**。
它必须自己解决「这条推送是真的吗」——见 §5.3。

---

## 5. 签名与安全

### 5.1 支付签名（我们 → 微信）

~~~
paySig = hex( hmac_sha256( appKey, uri + '&' + signData ) )
~~~

- wx.requestVirtualPayment 的 uri 固定填 requestVirtualPayment
- 调服务器 API 时 uri = 接口路径（如 /xpay/query_order），**不带 query string**
- appKey 分**沙箱**与**现网**两把，由 env 决定（env=1 → 沙箱，env=0 → 现网）
- ⚠️ 参与签名的 signData 必须**与实际发出的 body 逐字节一致**（序列化差异会直接导致签名错）

### 5.2 用户态签名（我们 → 微信，代表某个用户）

~~~
signature = hex( hmac_sha256( sessionKey, signData ) )
~~~

⚠️ sessionKey 只能从 auth.code2Session 拿，而我们的 callContainer 通道**没有它**。
⇒ 设计：下单接口接受一个可选的 loginCode；没有有效 session_key 时，
   端侧先 wx.login 拿 code 一起传上来，服务端换一次 session_key 并落库。
   （这也是「用户态签名」这个名字的意思：它代表的是**这个用户**同意这笔操作。）

⚠️ 待核实：**是否每个服务器接口都需要用户态签名**。官方示例只对 query_user_balance 演示了两种签名。
实现第一步就是拿沙箱环境把 query_order 单独打一遍，确认要哪几个签名 ——
**先证明链路通，再写业务**（同「先探针后实现」的既有做法）。

### 5.3 发货推送怎么验真

推送会打到我们的公网 URL，所以**任何人都能往这个地址 POST**。三层防线：

1. **查单为准**：收到推送后，用 out_trade_no 调 query_order 反查一次，以微信的答复为准发货。
   （最贵但最稳；对付伪造推送一步到位）
2. **金额对账**：推送里的 ActualPrice 必须 == 订单快照的 price_fen，不一致就**不发货**并告警
3. **状态机**：只有 pending 的订单能变 paid；已 paid 的直接返回成功（幂等）

⚠️ 恒等式的另一半：推送里的 OutTradeNo 必须**在我们库里存在**且属于这个 OpenId ——
   不存在就丢弃 + 告警（伪造或串环境的典型特征）。

### 5.4 响应格式（这个错了会被重推 15 次）

~~~
JSON：{"ErrCode": 0, "ErrMsg": "success"}
XML ：<xml><ErrCode>0</ErrCode><ErrMsg><![CDATA[success]]></ErrMsg></xml>
~~~

⚠️ 格式不对（或返回空但平台不认）→ 按 2/4/8/16… 的间隔**最多重推 15 次**。
所以「重推」是常态而不是异常：处理逻辑必须**天然幂等**，且**永远返回成功**，
除非我们真的希望它再推一次（比如自己数据库挂了）。

---

## 6. 对账、退款、边界

### 6.1 三种「没到账」

| 现象 | 处理 |
|---|---|
| 用户付了、推送没到 | 用户在 me/energy 点「我付了但没到账」→ 我们 query_order → 补发 |
| 用户下单了、一直没付 | 订单停在 pending。⚠️ 用户下次下单会生成**新单号**，旧单自然烂掉（不影响余额）|
| 推送到了、我们写库失败 | 返回**非 0**（让平台重推）—— 这是唯一该返回失败的情况 |

⭐ 还可以加一条**惰性兜底**：进 me/energy 页时，把 5 分钟前仍 pending 的订单各查一次单。
不做定时任务（本项目没有定时器基建），复用用户自己的访问频率。

### 6.2 退款

| 端 | 规则 |
|---|---|
| 安卓等 | 我们可调 /xpay/refund_order 主动退；180 天内的退款平台退还手续费 |
| **iOS** | ⚠️ **Apple 支付不允许开发者主动退款**。用户只能去 App Store 申请，Apple 会向我们发起**最多 3 次退款问询**（xpay_subscribe_ios_refund_query_notify），我们要在 **3 秒内**应答，否则平台替我们回「不确定」|

⇒ 必做：实现退款问询的应答接口（可返回「已发货、拒绝退款」或「放过」）。
⚠️ **3 秒**是很紧的：接口里不能做任何慢查询。默认策略建议：**已发货就拒绝**（附凭据 = 该订单的评测已消耗），
因为我们的商品是**即时消耗型**的（买了就用来打分）。

### 6.3 能量被消耗后退款怎么办

建议（MVP）：**钱退、能量扣回；余额不够就扣成负数**（负数余额 = 欠账，下次充能先补上）。
理由：拒绝退款的体验更差，而扣成负数在系统里是自洽的（余额本来就是整数，允许负）。
⚠️ 这条要你拍板（见 §10 待定 3）。

---

## 7. 客户端：pages/me/energy

放在 me/ 下（**只给自己看**，与对外展示的 pages/profile 相反）。

| 区块 | 内容 |
|---|---|
| ① 余额 | 大字 ⚡ N 点 + 一句「每次挑战消耗 2 点」+ 「每天补到 3 点」 |
| ② 充值 | 三张卡片（点数 / 价格 / 单价 / 角标「省 N%」）；点了就走 §4.1 的链路 |
| ③ 流水 | 时间 + 变动（+300 / −2）+ 原因文案 + 余额（可选）|
| ④ 环境提示 | 沙箱（payEnv = 1）时页面顶部**明说**「不会真扣钱」—— 测试期最容易犯的错觉就是「我在花钱」 |
| ⑤ 兜底 | 支付成功后**等余额到账**（最多试 3 次）→ 还没到就提示「到账可能要几秒，下拉刷新」，**不说失败** |

⭐ **入口**：用户面板名字下面那行「⚡ 能量 N 点 ›」（用 `catchtap` 单独接 ——
外面整块是「改资料」，不阻止冒泡会连带跳过去）。

⚠️ 定价卡片的价格**从接口来**，不写死在小程序里（理由见 §2.4）。
⚠️ 余额也**不从端侧加**：支付成功后不能直接 +N，只能「等推送把货发到、再刷新」——
直接加就是在端侧造第二份真相，推送失败时用户会以为到账了、刷新一下又没了。

#### ⚠️ 一个必须绕过的 typings 陷阱（否则签名必错）

基础库的 `d.ts` 把 `wx.requestVirtualPayment` 的 `signData` 标成了**对象**
（`SignData` 接口），而官方文档写的是「该参数需以 **string** 形式传递」。

⇒ 只能**原样传服务端签好的那个字符串**，并显式绕过类型：

~~~ts
const payOption = order.payData as unknown as WechatMiniprogram.RequestVirtualPaymentOption
wx.requestVirtualPayment({ ...payOption, success, fail })
~~~

⚠️⚠️ 千万别为了迁就类型去 `JSON.parse` 成对象 —— 那等于让平台**重新序列化**一次，
键顺序或空格一变，签名就对不上，报 **-15006（支付签名错）**，
而排查方向会先怀疑签名算法和 AppKey（服务端那 10 条签名测试还全绿）。

流水的原因文案（reason → 中文）全部由端侧映射，**服务端返回 reason 原文**：

| reason | 文案 |
|---|---|
| purchase | 充值 |
| daily_topup | 每日补足 |
| challenge_hold | 挑战（冻结）|
| challenge_release | 挑战失败退回 |
| streak_unfreeze_7 等奖励码 | 连战奖励 等（规则表里配置的名字）|

⚠️ hold / release 是**成对**的：挑战成功时 hold 之后会被结算成实扣，失败时 release 退回。
流水页要不要把这一对折叠起来显示，是一个展示问题 → §10 待定 4。

---

## 8. 本地开发怎么办

| 环节 | 本地（wx.request + 伪 openid 或真 openid）|
|---|---|
| 商品列表 / 下单落库 | ✅ 可以（用 PAY=mock 走假 payData）|
| 真拉起支付 | ❌ 不行 —— 必须真机 + 真环境（这也是 AGENT.md 既有结论）|
| 发货推送 | ✅ 可以：本地直接 curl 打 /api/pay/notify 模拟推送 |

⇒ 加一个 env 开关 **PAY=mock | wxpay**（与 ENGINE=mock | xfyun 同一套写法）：
mock 时下单返回一个假 payData，并**立即**走一遍「推送处理」的内部函数（这样本地能整体联调）。

---

## 9. ⭐ 你要去配置的通道（清单）

### 9.1 微信支付侧（新开虚拟支付商户号）

1. **确认小程序主体**：企业 / 事业单位 / 个体工商户（个人主体另有一条路，但要求服务类目含「工具」，且月限额 10 万）
2. **MP 后台 → 左边栏【虚拟支付】→ 开通**：签协议 → 提交营业执照 / 对公提现账户 / 支付管理员 → 审核 1–7 个工作日
3. **账户验证 + 扫码签约**（管理员微信扫码）→ 约 1–2 个工作日后状态变「已签约」
4. ⚠️ **这一步会新开一个二级商户号** —— 你说的「现成的商户号」用不上（官方文档原文：
   「可通过小程序管理后台 - 虚拟支付模块，**开通一个新的微信支付商户号**」）
5. **道具管理**：建三个道具（10 / 300 / 3000 点）并**发布到现网版本**，记下三个**道具 ID**
6. **基础配置**里拿三样东西：**AppID / OfferID / 现网 AppKey**（另有沙箱 AppKey）
7. ⚠️ **iOS 额外条件**：配置**小程序简称**（Apple 要展示 display name），然后在虚拟支付-基础配置里开通 iOS 支付
8. **发货推送（消息推送）URL**：填我们的 https 回调地址（云托管域名 + /api/pay/notify）

### 9.2 我们这边

| 要配的东西 | 值 / 来源 |
|---|---|
| XPAY_OFFER_ID | 虚拟支付-基础配置 |
| XPAY_APP_KEY / XPAY_SANDBOX_APP_KEY | 同上（**两把**，按 env 选）|
| **XPAY_ENV** | ⚠️ **不用手填** —— deploy-cloud.mjs 结构性决定：**dev = 1（沙箱）/ prod = 0（现网）** |
| 三个道具 ID | 道具管理（同步进 goods.xpay_product_id）|
| （可选）沙箱道具 ID | 若沙箱与现网的 ID 不同，填 XPAY_PRODUCT_ENERGY_*_SANDBOX |
| 回调 URL | https://<云托管域名>/api/pay/notify |

⚠️ 上面几项都是**密钥级**，走既有的 tools/gh-secrets.mjs 一起进 GitHub Secrets + 服务环境变量。

#### ⭐⭐ 为什么 XPAY_ENV 必须由 deploy 脚本决定，而不是写进 .env

`env` 的代码默认值是 **0（现网）**，而「忘了配」在 dev 上的后果是——
**测试的时候真扣钱**，而且它**不会报错**：支付照常成功，只是花了真钱。
这类「静默花真钱」正是最该用结构而不是纪律去防的东西，所以它和 ENGINE / SEED_ON_START
一样写死在 deploy-cloud.mjs 里：

~~~
XPAY_ENV: target === 'dev' ? '1' : '0'
~~~

⭐ 配套的自查：**`/health` 里有 `pay` 块**，一眼能看出当前是沙箱还是现网、
该环境的 AppKey 和三个道具 ID 配没配全 ——
`{"mode":"xpay","env":1,"envName":"沙箱","offerId":true,"appKey":true,"productIds":{...}}`

⚠️ 沙箱的两个前提（**待实测核实**）：

1. 道具在微信侧有「**开发版本**」与「**现网版本**」两种状态，ID 未必相同 ——
   不同就填 `_SANDBOX` 那几个变量（dev / prod 是两个独立的库，各存各的，不用在表里分环境）
2. 腾讯规定「**现网版本的 env 只能是 0**」（报错 -15011）⇒
   **沙箱只能用开发版 / 体验版测**，正式版试不了

### 9.3 ❌ 不需要配的（省得你去折腾）

- ❌ 不需要 APIv3 密钥、apiclient 证书（虚拟支付用 appKey 签名，不走证书）
- ❌ 不需要在云托管控制台配「微信支付配置」（那是**普通**微信支付那条路）
- ❌ 不需要开「开放接口服务」（虚拟支付不走 api.weixin.qq.com 的云调用封装）
- ❌ 不需要实物电商那套发货信息管理 / 确认收货组件

⚠️ 最后一条要**核实一次**：我们原以为要走云托管封装（那才需要开放接口服务）。
虚拟支付是独立的一套 HTTP API（/xpay/*），按文档不需要开放接口服务。会上线前用沙箱验一次。

---

## 10. 待你确认

> ⚠️ **待确认项已统一收在 [plan.md](../../plan.md) A2–A5 / A8–A9（含建议）。**
> 这里只留**已经定下来的** —— 同一个问题两处各写一半，迟早只更新一边。
> 已定的一条：价格 **¥1 / ¥20 / ¥180**（两条硬约束见 §2.2）。

**已定**（本轮讨论）：

- 卖能量包（10 / 300 / 3000），商品表用 kind 预留解冻卡
- 保存消费流水（payments）与能量流水（energy_ledger：充值 / 消耗 / 奖励三类都在里面）
- 新增 pages/me/energy（余额 + 充值 + 流水）
- 走**小程序虚拟支付**、**道具模式**（不是代币模式）—— 三条理由见 §1.3：
  ① 余额只能有一份真相 ② 代币的固定兑换比例表达不了阶梯定价 ③ 每日惰性补足 max(energy,3)
  这个语义微信侧没有

---

## 11. ⭐ 开通执行清单（你那边，按顺序）

### 11.0 先定这一条（会卡住第 5 步）

✅ **已定：¥1 / ¥20 / ¥180**（10 / 300 / 3000 点）。

这一节保留下来，是因为「价格必须在建道具之前定」这条经验仍然成立：

- 道具价格是**在微信侧配置**的，安卓 / iOS 双端通用（不能两端两个价）
- 道具虽然可以「编辑后重新发布」，但改价要走一次编辑 + 发布（可能带审核），
  而**订阅道具根本不可编辑** —— 养成「先定价、再建道具」的习惯

⇒ 我的建议仍是 **¥1.00 / 10 点**（理由见 §2.2：iOS 最低支付 1 元）。

### 11.1 开通前自查（不满足会被驳回，别白等 1–7 天）

| # | 条件 | 我们 |
|---|---|---|
| 1 | 小程序**已认证** | 需你确认 |
| 2 | **服务类目含「工具」** | ✅ **确认是「工具」** —— 个人主体开通虚拟支付本来就要求这个类目 |
| 2b | 主体类型：**个人** / 个体户 / 企业 | ⚠️ **仍需确认** —— 两条路的入口、材料、提现方式都不同，见 11.4 |
| 3 | 主体信息完备（有缺失要先补） | 需你确认 |
| 4 | **对公提现账户**（用于结算提现） | 需你确认 |
| 5 | 支付管理员（非法人时要走「账户验证」） | 需你确认 |
| 6 | 要 iOS ⇒ **小程序简称**已配置 | ⚠️ Apple 要展示 display name，没配开不了 iOS 支付 |

### 11.2 开通流程（官方五步 + 等待）

1. MP 后台 → 左边栏【**虚拟支付**】→ 点【开通】
2. 阅读并勾选协议 → 填营业执照 / 提现账户 / 支付管理员
3. 等审核 **1–7 个工作日**
4. 账户验证（支付管理员是法人则跳过）→ **管理员微信扫码签约**
5. 再等 **1–2 个工作日** → 状态显示「已签约」= 二级商户号开通成功

⚠️ 第 4、5 步不一定当场完成，可以关掉页面，下次进来接着做。

### 11.3 开通后要做的三件事

| # | 做什么 | 在哪 | 产出 |
|---|---|---|---|
| 1 | 建三个**道具**（10 / 300 / 3000 点）并发布到现网 | 虚拟支付 → 基础配置 → 道具管理 | **三个道具 ID** |
| 2 | 抄下四个值 | 虚拟支付 → 基础配置 | **OfferID**、**现网 AppKey**、**沙箱 AppKey**、**AppID** |
| 3 | 填**发货推送 URL** | 道具管理里的推送配置 | 见下 |

推送 URL：

~~~
dev ： https://jupin-219743-12-1258596499.sh.run.tcloudbase.com/api/pay/notify
prod： <prod 服务域名>/api/pay/notify   ← prod 服务还没部署过，域名待定
~~~

⚠️⚠️ **推送 URL 大概只能配一个地址**（官方没有说可以按环境分开配）—— 那 dev 和 prod 就会抢同一个回调。
⇒ 【待核实】开通后进基础配置看一眼「发货订阅」是不是能分环境 / 分沙箱；
如果不能，我们靠 §6.1 的 **query_order 主动查单兜底**：dev 收不到推送也能跑，只是发货慢一点。
（我们的设计本来就把查单当兜底而不是可选，所以这条不阻塞。）

### 11.4 个人主体 vs 企业/个体户（两条路差别很大）

| | **个人主体**（我们类目正好符合）| 企业 / 事业单位 / 个体工商户 |
|---|---|---|
| 入口 | ⚠️ **微信开放平台** → 支付与交易 → 虚拟支付（官方个人页写的是这里）| MP 后台（mp.weixin.qq.com）→ 左边栏【虚拟支付】|
| 材料 | 个人身份信息 + **提现账户** + 支付管理员 | 营业执照 + **对公**提现账户 + 支付管理员 |
| 审核时长 | ⭐ **一般 5 分钟内** | 1–7 个工作日 |
| 服务类目要求 | ⚠️ **必须含「工具」** | 无此要求 |
| 月支付限额 | ⚠️ **10 万元 / 月** | 无此限制 |
| 不支持的服务 | 需电信业务资质的那几类（电子邮件、语音信箱、存储转发、信息发布平台、信息搜索查询等）| — |
| 自动续费订阅 | ⛔ **不支持**（官方明确：仅企业/政府/事业单位/社会组织，不含个人和个体工商户）| 满 DAU 门槛后可申请 |
| 费率 | Android 1% / iOS 12% | 同左 |

⭐ 两点提示：

1. **入口两处不一致**（个人页说开放平台、企业页说 MP 后台）—— 以你实际能看到的入口为准，
   两边都翻一下，别因为没找到就以为没资格。
2. 我们**卖的是能量（虚拟代币）**，落在第 1 条的虚拟商品定义里，与类目无关；
   类目「工具」在个人主体这条路上恰好是**必需条件**，属于刚好对上。

### 11.5 要不要为了省事去开个人小程序？

⭐ 结论前置：**先别动。**判断标准不是「哪条审核快」，而是**「一次性麻烦」换「永久限制」值不值**。

| | 个人主体 | 企业 / 个体户 |
|---|---|---|
| 开通耗时 | ⭐ 5 分钟 | 1–7 工作日 + 验证 + 签约 |
| 材料 | 个人身份 + 提现账户 | 营业执照 + 对公账户 |
| 成本性质 | **一次性**（填表）| **一次性**（填表）|
| 月支付限额 | ⚠️ **10 万 / 月（永久天花板）** | 无 |
| 自动续费订阅 | ⛔ **永远不能开** | 可申请（DAU 门槛另说）|
| 部分服务类型 | 禁入（需电信资质那几类）| 可 |

⚠️ 关键：企业路那点「麻烦」是**一次性**的（营业执照和对公账户本来就有的话，就是填表 + 等审核），
而个人路那三条是**永久**的。**用永久天花板换几天的等待，不划算** ——
除非你手上根本没有营业执照 / 对公账户。

#### 想换主体？先看这条路通不通

小程序**主体变更**（不是新注册）可以保留 **AppID 和 openid 不变** —— 用户完全无感。
但官方条件里有一条卡死我们这种想法：

> **接收方不能是个人主体**（必须为企业或组织机构）

⇒ 所以：

| 方向 | 能走通吗 |
|---|---|
| 个人 → 企业 | ✅ 可以（要营业执照、法人扫码或公证、300 元审核费、3–7 工作日）|
| **企业 → 个人** | ⛔ **走不通**（个人不能做接收方）|

⚠️ 也就是说「我现在是企业主体，想换成个人省事」**只能新注册一个小程序** = **新 AppID**。
（来源：微信开放社区 + 第三方流程梳理，标注为**待核实**，真要走这条我去查官方原文。）

#### 新 AppID 的代价（不是钱，是工序）

| # | 要重做的事 |
|---|---|
| 1 | 重新注册 + 微信认证 + **ICP 备案** |
| 2 | 云托管：dev / prod 两个环境、服务 jupin、对象存储桶 —— 重绑或按「资源复用」改造 |
| 3 | 已配好的 13 个 GitHub Secrets 里的 WXCLOUD_ENV_ID 等要换一轮 |
| 4 | 代码/配置：project.config.json 的 appid、config.ts 里的 MP_DEV_ENV_ID / MP_PROD_ENV_ID |
| 5 | ⚠️ **openid 全变** —— openid 是「小程序维度的用户 ID」，换了 AppID，
  同一个人就是另一个人：连战、成长值、能量全部归零 |

✅ **一个好消息**：`prod` **从来没部署过**（数据库 `jushuo` 在 prod 环境里根本不存在），
所以**现在没有任何真实用户**，换 AppID 的数据代价 ≈ 0。
⭐ 但也因为如此：**要换就现在换，上线之后就再也换不起了。**

#### 决策快捷键（三条，按顺序问自己）

| # | 问题 | 是 → | 否 → |
|---|---|---|---|
| 1 | 12 个月内月流水可能超过 10 万吗？ | **企业** | 继续问 |
| 2 | 以后要做包月 / 年费（自动续费）吗？ | **企业** | 继续问 |
| 3 | 拿得到营业执照 + 对公账户吗？ | **企业**（那点麻烦是一次性的）| **个人** |

⚠️ 但无论哪条：**微信认证 + ICP 备案都省不掉** —— 个人虚拟支付的页面上明确要求
「已完成小程序认证、备案」。这两步的时间是省不掉的，能省的只是「审核虚拟支付」那几步。

### 9.3.1 ⭐ 道具批量导入（三个 ID 已经定下来了）

道具管理支持**批量添加**（上传 xlsx）。官方模板的 5 列是：
**道具id / 道具名称 / 道具图片 / 道具价格 / 备注**，其中：

| 列 | 限制（官方模板里的原话）|
|---|---|
| 道具 id | 必填，不超过 **32 位英文字符** |
| 道具名称 | 必填，不超过 **10 个字** |
| 道具图片 | 非必填（只有 id / 名称 / 价格标了必填）|
| 道具价格 | 必填，**需为整数**，不超过 **10000 元** ⚠️ |
| 备注 | 非必填，不对外展示，不超过 50 个字 |

⭐ 三个道具的文件由 `tools/make-goods-template.ts` 生成（**从 ENERGY_PACKS 推导，不手抄**），
产出在 `docs/design/assets/` 下：

| 商品码 | 道具 id | 道具名称 | 价格 |
|---|---|---|---|
| energy_10 | `energy10` | 10点能量 | ¥1 |
| energy_300 | `energy300` | 300点能量 | ¥20 |
| energy_3000 | `energy3000` | 3000点能量 | ¥180 |

⚠️ **道具图片留空**（非必填）；备注里写了 `goods.code=…`，将来对账时能一眼对回我们的商品表。

⭐ 三档都是**整元**，与官方「需为整数」天然一致 ⇒ 只产出一个文件 `jupin-道具批量导入.xlsx`。
（若将来有档位不是整元，工具会自动产出两个版本，并打印出「代码里该改哪几个数」——
因为这个耦合是真实的：道具价格在微信侧，商品价格在我们库里，**两者必须同值**。）

### 11.6 四个值怎么给我

⚠️ **不要贴在聊天里**（会进对话记录）。填到本机 `.env` / `.env.dev` 对应位置即可：

~~~
XPAY_OFFER_ID=...
XPAY_APP_KEY=...            # 现网
XPAY_SANDBOX_APP_KEY=...    # 沙箱
XPAY_PRODUCT_ENERGY_10=...  # 道具 ID
XPAY_PRODUCT_ENERGY_300=...
XPAY_PRODUCT_ENERGY_3000=...
~~~

我用 tools/gh-secrets.mjs 同步到 GitHub Secrets + 服务环境变量（和现有 13 个 secret 同一条路）。

---

## 附：和既有文档的关系

- growth-and-energy.md §2 里「充值：起步单位 10 点，reason='purchase'，ref=paymentId」——
  实现时 ref_id 用 **out_trade_no**（比自增 paymentId 更适合做对账键），文档要说一句
- growth-and-energy.md 里「users.memberUntil / subscriptions 表保留不动不参与判定」——
  **本设计不需要动订阅**：能量是单次消费，不做包月（保持既有结论）