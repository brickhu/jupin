# 句拼 · 技术决策记录（SPEC）

> ⚠️⚠️ **这个文件不是「现状说明」，是「决策记录」。**
> 它只回答一个问题：**为什么这么选**（以及什么条件下该重估）。
>
> | 想知道 | 去哪看 |
> |---|---|
> | 现在到底是什么行为 / 什么规则 | **代码及其注释（唯一真相）** |
> | 表结构 | [apps/server/src/db/schema.ts](apps/server/src/db/schema.ts) |
> | 业务、名词、概念 | [prd.md](prd.md) |
> | 要做什么、做到哪了 | [plan.md](plan.md) |
> | 工程索引、怎么跑 | [AGENT.md](AGENT.md) |
>
> **规矩：能派生的不写在这里。** 字段、常量、路径、阈值、DDL 一律指向代码 ——
> 在这里抄第二份，就是漂移的种子（本文件曾经抄过一份表结构，后来被抓到三处与代码不符）。
> 只有「代码里没地方放、但将来一定有人问为什么」的东西才留下。

---

## 一、三条技术原则

### 原则 1：云端只买「一个分数」

```ts
score(audio: Buffer, refText: string) → { total: number }
```

**音高、流利度、进度、预检、回放、上色、对比——全部端侧自建。**
云端返回的词级/句级数据是**可选副产品**：有则用于精修，无则端侧兜底。

**推论**：换引擎成本 ≈ 0；将来端侧模型可用时，直接替换 `score()` 实现。

### 原则 2：高频动作边际成本为 0

只有「提交检测」调用云端。录音/重录纯本地。

### 原则 3：内容以静态资源分发，不经后端 API

内容（正文 / 标准音 / 词级数据）→ 静态资源，小程序端直拉。
路径由内容 id 推导，**库里不存路径**（曾经存过一列，删了）。

---

## 二、平台决策：微信小程序

### 依据

| 因素 | 判断 |
|---|---|
| ⭐ **竞技场需要人口密度** | 人口密度需要传播效率。小程序"点开就玩"，App 要下载 → **漏斗差一个数量级** |
| **端侧必需能力** | 小程序全都能实现（见下），只有两项是工程量问题 |
| **端侧模型** | 唯一的 App 独有优势，但本来就是 P2 |
| **支付** | 小程序内支付现成 |

### 端侧能力可行性

| 能力 | 小程序 | 说明 |
|---|---|---|
| 录音（PCM 16k） | ✅ | `getRecorderManager` + `onFrameRecorded` 给实时帧 |
| 本地预检（VAD/能量/时长） | ✅ | 纯 JS 信号处理 |
| ASR 可识别性 | ✅ | 微信同声传译插件（免费） |
| 实时进度 | ✅ | 插件 `onRecognize` |
| 音高曲线 | ⚠️ ✅ | 需手写 FFT/自相关（或 WebAudioContext 的分析节点） |
| 语速 / 停顿 | ✅ | 纯算法 |
| 点词回放 / A/B | ⚠️ ✅ | 标准音用预切文件；用户音用 `seek` |
| 逐词上色 | ✅ | 端侧与标准音对比 |
| 端侧音素模型 | ❌ | `createInferenceSession` 为 Beta + 算子受限（**P2 才需要**） |

### 风险隔离

对微信能力的依赖只有两处，且都有兜底：

| 依赖 | 兜底 |
|---|---|
| 同声传译插件 | 退回纯本地时长/能量判定；实时进度退回本地 VAD |
| 支付 | 小程序虚拟支付 |

### ⭐ 转 App 的触发条件

| 条件 | 说明 |
|---|---|
| 数据显示流失主因是"反馈不够精确/实时" | 而非"不知道这个产品" |
| 端侧音素级诊断成为核心差异化 | 竞品都在做而小程序做不了 |
| 已有足够用户量支撑下载转化 | 例如 DAU 过万 + 自然搜索流量 |

**因为架构是"云端只要分数"，转 App 的成本主要在 UI 重写，不在能力重建——这个决定可逆。**

---

## 三、系统架构总览

```
┌─ 小程序端（产品本体）───────────────────────────────┐
│  内容层  正文 / 标准音 / 词级数据 ← 静态资源          │
│  音频层  录音 · 预检 · 播放 · 逐词回放                │
│  交互层  打分结果 · 榜单 · 连战 · 能量                │
└──────────────────┬────────────────────────────────┘
                   │ ① 提交 (audio + articleId)
                   │ ② 榜单 / 我的数据
┌──────────────────▼────────────────────────────────┐
│  Node 服务（微信云托管）                             │
│  · 鉴权(openid) · 能量门禁 · 打分 · 榜单 · 奖励       │
│  · ScoreEngine 薄适配层                            │
└──────┬──────────────────────┬─────────────────────┘
       │                      │
┌──────▼──────┐      ┌────────▼────────┐
│ 讯飞 ISE     │      │ MySQL 8         │
│ (只买分数)   │      │ 成绩/用户/能量    │
└─────────────┘      └─────────────────┘

内容生产流水线（离线，独立）→ 对象存储 / 静态资源
```

---

## 四、技术栈选型

| 层 | 选择 | 理由 |
|---|---|---|
| **小程序框架** | **原生小程序 + TypeScript** | 核心是音频处理 + 高频动画，跨端框架恰在这两处最易出坑；且不做多端 |
| **音频采集** | `getRecorderManager` + `format:'PCM'` + `frameSize` + `onFrameRecorded` | 直出 16k/16bit/单声道，零转码 |
| **状态** | 极简自研 store | 状态很少，不引入重框架 |
| **图表** | canvas 2d 手绘 | 只画柱状图，ECharts 过重（主包 2MB） |
| **分包** | 主包只放首页 + 朗读页 | 主包限制 2MB |
| **服务端运行时** | **微信云托管**（Node 容器） | 支持 WebSocket、免域名备案、免运维 |
| **服务端框架** | **Hono** | 轻、快、TS 友好 |
| **数据库** | ⭐ **MySQL 8**（云托管内置） | 部署目标只提供 MySQL；本模型不用任何 PG 特有特性 |
| **ORM** | **Drizzle**（`drizzle-orm/mysql2`） | 方言可换，schema 即类型 |
| **对象存储** | 微信云开发存储 / 腾讯云 COS | 标准音与录音 |
| **缓存** | **不需要 Redis** | 榜单查询走复合索引 + 聚合，毫秒级 |

> ⚠️ 端侧**评分分析**（音高曲线 / DTW 逐词对齐）**已下线**（见 prd）。
> 上面那条 `wx.createWorker` 独立线程属于当时的方案，**没有实现** —— 活见 plan.md。
> 端侧现在只做录音、预检、播放；具体以代码为准。

---

## 五、为什么是 MySQL 而不是 PostgreSQL

**原设计选的是 PostgreSQL，后来推翻了。** 决定因素不是技术偏好，是**部署目标**：

- 微信云托管**只提供 MySQL 5.7/8.0**，没有 PG；
- 官方明确「不支持用来部署数据库/Redis 等有状态服务」——也不能自己塞一个 PG 容器（容器无持久化存储）；
- 变通路径只有「腾讯云 TDSQL-C PostgreSQL + 服务配 VPC 打通」，要多一份账单、多一套运维、跨 VPC 连接。

而我们这个数据模型**不用任何 PG 特有特性**：无 jsonb、无数组、无 CTE、无窗口函数。
既然如此，用它换「同环境 VPC 内网库 + 免运维 + 一张账单」是明显划算的。

> ⚠️ 唯一损失：MySQL 没有 `INSERT ... RETURNING`。受影响的只有两处，
> 已分别用 `INSERT IGNORE` + 回查、以及 `ON DUPLICATE KEY UPDATE` 解决
> （见 `services/user.ts` 与 `routes/submissions.ts` 的注释）。

### ⚠️ 时间列一律用 DATETIME，不用 TIMESTAMP

MySQL 的 `TIMESTAMP` 只到 **2038 年**；而 Drizzle 的 `datetime` 没有 `defaultNow()`，
所以默认值写成显式 SQL。另外 `DATETIME` **不存时区** —— 全链路按 UTC 读写
（连接池 `timezone: 'Z'` + 容器 `TZ=UTC`），否则「上次挑战在什么时候」会整体偏移。
（踩过一次：往库里写时间时忘了给时区，结果整体差 8 小时。）

---

## 六、为什么内容走 CDN / 静态资源

| | 后端 API | 静态资源 |
|---|---|---|
| 后端负载 | 每次读内容都查库 | **零** |
| 分发成本 | 数据库 + 服务器 | **几乎免费** |
| 响应速度 | 取决于后端 | **边缘节点** |
| 离线能力 | ❌ | ✅ **可缓存到本地** |

### ⭐ 单词音频预切

在**生产阶段**就把每个词的音频切好，而不是运行时用 `seek()` 动态切。

**理由**：点词回放和 A/B 对比是核心体验，精度重要，而 `InnerAudioContext.seek()` 精度不够可靠。
预切后播放就是普通音频文件，简单且精确。

> 目录布局、路径推导、音频的「有没有」以谁为准 —— **全部以代码为准**
> （`packages/shared/src/content-path.ts`、`apps/server/src/services/content.ts`、`standard-audio.ts`）。
> 本文件曾经抄过一份目录清单，已经过时并删除。

---

## 七、榜单：不物化，从 submissions 现算

**没有物化榜单表。** 核心是一个「每人最高分」的派生表，所有榜单查询都基于它。

**排序规则**：分数降序，同分按**最早提交时间升序**（先到者优先）——这样「刚刚超过你」的语义才准确。

> ⚠️ 严格说「先到」应是「**先达到该分数**」，但那是 correlated 子查询；
> 这里用「最早提交时间」近似，对榜单语义影响可忽略。已记在 `services/leaderboard.ts` 的注释里。

**为什么不做物化表**：参与人数 / 最高分 / 我的排名都是**聚合的结果**。
存一份就是第二份真相，而排序与并列判定会随每一次提交变化 —— 让它漂移的代价远大于聚合的开销。

**榜心而非全榜**：揭晓页只返回"附近 5 条 + 人数 + 我的排名"，一次查询、数据量极小。

---

## 八、引擎接入层

```ts
interface ScoreEngine {
  score(audio: Buffer, refText: string): Promise<ScoreResult>
}

interface ScoreResult {
  total: number               // ⭐ 唯一必需：0–100
  words?: WordScore[]         // 可选：有则用于精修，无则端侧兜底
  sentences?: SentenceScore[] // 可选
}
```

**第一版实现 `XfyunEngine`。** 具体的参数、字段映射、踩坑，
**全部在 `apps/server/src/engines/xfyun.ts` 的注释里，并由 `xfyun.test.ts` 钉住** ——
那些是"不知道就会再踩一次"的知识，放在代码旁边才不会与实现脱节。

**还有一条文档级陷阱值得记在这里**：网上易搜到的《语音评测 API 文档》是**普通版**文档
（已下线），示例分制与**流式版不同** —— 照它写代码会踩坑。

### 引擎选型结论

| 引擎 | 结论 |
|---|---|
| **科大讯飞 ISE** | ⭐ **选它**：最便宜 + 有免费额度 + 已踩完坑 |
| 腾讯云 SOE | ❌ 不选：其增值能力（实时/音素/IPA/SDK 工程）**我们要么端侧自建、要么不需要**；且按文本长度计费、免费额度少 |
| 驰声 Chivox | 备选：唯一明确支持实时流式 + 音素级，但贵数倍 |

> **注意**：上一轮曾把腾讯云 SDK 的"静音检测/分片/重试"当作选型优势——**这是错的**，
> 那些是本该端侧自建的工程实现，不构成引擎能力差异。

---

## 九、内容生产的取舍

**人工不可替代的三件事**：选文 · 审核 · 抽查。

几条不随实现变化的取舍：

- **音标绝不能让 LLM 生成** —— 会幻觉且难发现；查表（ECDICT，MIT）。
- **词级释义：LLM 从「生成」降级为「选择」** —— 它从义项里挑语境义，不自己编。
- **难度是「朗读难度」，不是「阅读难度」** —— 绕口令词汇简单但朗读极难，按词汇定级会严重低估。
- ⭐ **难度全由 LLM 判，不算脚本特征**（2026-09 决定，废弃了原方案「脚本算特征 → LLM 综合判断」）：
  能算出来的那几个量（难音密度 / 连读点 / 弱读词数 / 音节数）**都只是代理指标** ——
  绕口令全是小学词却最难，说明真正的难点在**音素序列的切换模式**，不在可数的几个数上；
  而且算偏了会**误导**模型（把最难的绕口令算成最简单的一档）。
  ⇒ 判据只有一条：让模型「读」这句话，给四档 + 理由，人工审核。
  ⚠️ **校准手段（锚点样本）暂未采用** —— 先看实测（同一句多次定级稳不稳，见 [plan.md](plan.md) C6），不稳再加。
- **标准音与用户朗读的韵律条件必须一致** —— 否则 A/B 对比不公平。
- **音色是代码里的常量**，换音色 = 全库标准音一起换，必须是可评审、可回滚、跨环境一致的一处改动。

> 具体步骤、产物、校验规则见 `tools/pipeline/` 与它的 README —— 那是流程，不是决策。

---

## 十、成本模型

```
成本 = 提交次数 × 单价
```

**没有"实时会话"、"分片请求"、"音素解析"等隐藏成本项——一个变量。**

DAU 1000（免费 950 / 付费 50）估算：

| 项 | 计算 | 月成本 |
|---|---|---|
| 免费用户提交 | 950 × 1 次/天 | ¥110 |
| 付费用户提交 | 50 × 6 次/天 | ¥35 |
| **合计** | | **≈ ¥145** |
| 月收入 | 50 × ¥19.9 | ¥995 |
| **毛利** | | **≈ ¥850（85%）** |

---

## 十一、这个文件里删掉过什么（避免有人再抄回来）

| 曾经有 | 为什么删 |
|---|---|
| **数据库表结构 / 字段清单** | 与 `schema.ts` 是同一事实两处；被抓到三处与代码不符（凭空多一列、字段语义写错、迁移号对不上） |
| **静态资源目录布局** | 路径由 id 推导，实现在 `content-path.ts` / `content.ts` |
| **CDN 侧 JSON 的形状** | 类型与校验在 `packages/shared/src/types/content.ts` 与流水线里 |
| **讯飞 ISE 踩坑清单（5 条）** | 已在 `engines/xfyun.ts` 注释里、且被 `xfyun.test.ts` 钉住 |
| **端侧录音 + 分析流水线 / 预检分层** | 端侧评分分析已下线；存活的实现见代码 |
| **风险与验证清单** | 那是任务 → 已收编进 [plan.md](plan.md) C6 |
